COMPLETE UI REDESIGN DIRECTIVE — DISMD AI SECURE PDF STUDIO

Execute a complete visual redesign of `artifacts/api-server/app/static/index.html` as a single, self-contained HTML file using Tailwind CSS (CDN) and Google Fonts (Inter / IBM Plex Sans). Preserve ALL existing JavaScript backend logic, endpoint routes, event listeners, DOM element IDs, and API fetch calls intact.

1. BRANDING & HEADER (TOP NAVBAR):
   - Solid charcoal header (`#1A1A2E`).
   - Wordmark Left: "DISMD AI — Secure PDF Studio" (Inter bold, `#FFFFFF`) accompanied by a rich gold (`#C9A84C`) shield/lock icon.
   - Header Right: "Clear Session" styled as a subtle ghost button with a hover highlight.

2. SECURITY & PRIVACY BANNER:
   - Place a locked security banner directly beneath the navbar:
     "DISMD AI PRIVACY GUARANTEE: Zero-Cloud-Leak Node. All PDF processing is executed locally via PyMuPDF. Raw document binaries, text, and client PII are NEVER transmitted to external LLM APIs."

3. THREE-COLUMN / TWO-PANEL WORKSPACE LAYOUT:
   - Left Sidebar (Midnight Navy `#16213E`, width ~320px-360px):
     * Upload Zone: Designed as a "Secure Vault Drop" — dark background with a dashed gold (`#C9A84C`) border, gold lock icon, and clear "Drag & drop legal PDFs here" text.
     * Document List: Items styled like digital case management file folders — document icon, truncated title, page count badge, and a gold border highlight for the selected file.
     * Page Thumbnails: Displayed beneath the file list as shadow-backed cards with a gold selection ring on active pages.
   - Right Main Panel (Deep Charcoal `#1A1A2E`):
     * Tabbed Header: "AI Chat Assistant" and "Manual Controls" tabs.
     * Tab 1 (AI Chat): User bubbles right-aligned in gold/amber (`#C9A84C` text/accent); assistant responses left-aligned in dark cards with an "AI" badge.
     * Footer Privacy Notice in Chat: Permanent indicator — "Privacy Protected: Document content is never sent to AI (Parser receives file names and page counts only)."
     * Tab 2 (Manual Controls): Group actions into labeled cards ("Page Actions", "Document Actions"). Buttons feature dark card backgrounds with gold border lift on hover.

4. ACTION BAR & LEGAL DISCLAIMER (FOOTER):
   - Primary Action Buttons: "Download All" (Solid Gold `#C9A84C` with dark text) and "Download Selected" (Ghost style).
   - Legal Footer Disclaimer:
     "DISMD AI is a local document processing utility. It does not provide legal representation or advice. Users remain responsible for verifying document accuracy prior to judicial or state bar filing."

5. INTERACTIVE STATES:
   - Apply deliberate hover, active, and loading spinner states on all buttons during async operations.